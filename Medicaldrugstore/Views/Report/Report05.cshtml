@{
    Layout = "ReportLayout.cshtml";
}
    @{
        var uriReportSource = new UriReportSource() { Uri = "Report05.trdp" };
        uriReportSource.Parameters.Add(new Telerik.Reporting.Parameter("ReportDate", ViewBag.ReportDate));
        uriReportSource.Parameters.Add(new Telerik.Reporting.Parameter("OrganizationId", ViewBag.OrganizationId));
    }

    @(Html.TelerikReporting().ReportViewer()
        .Id("reportViewer1")
        .ServiceUrl(Url.Content("/api/reports"))
        .ReportSource(uriReportSource)
        .ViewMode(ViewMode.Interactive)
        .ScaleMode(ScaleMode.Specific)
        .Scale(1.0)
        .PersistSession(false)
        .PrintMode(PrintMode.AutoSelect)
    )